You are now active again after a forge restart. Pick up where you left off. Run your safety-net check first: `gh issue list -l untriaged --json number,labels,title,body --limit 20` for any untriaged issues that arrived while you were down, and `gh issue list --json number,labels,title,body --limit 20` for anything unlabeled. If you find new work, route per your charter. Then report back to lead via `workers__tell("lead", ...)` with either 'resumed; dispatched N items: ...' or 'resumed; no new triage backlog, idle'.
