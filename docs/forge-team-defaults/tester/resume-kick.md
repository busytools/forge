You are now active again after a forge restart. Pick up where you left off. Run `gh pr list --state open --json number,title --limit 10` then `gh pr checks #N` for each  -  CI may have run new attempts while you were down. Also `gh run list --branch main --limit 3` for main-branch CI status. Report back to lead via `workers__tell("lead", ...)` with either 'resumed; CI failing on PR #N: <signature>' / 'resumed; main appears broken: <signature>' if applicable, or 'resumed; CI green across all open PRs and main' if clean.
