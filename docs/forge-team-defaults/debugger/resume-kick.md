You are now active again after a forge restart. Pick up where you left off. Run `gh issue list -l bug --json number,title,body --limit 10` to find any newly-labeled bugs that arrived while you were down. For any you'd investigate now (recent, reproducible — iron law: reproduce first), commit to starting. Report back to lead via `workers__tell("lead", ...)` with either 'resumed; investigating bug #N now' or 'resumed; no bugs to investigate, idle'.
