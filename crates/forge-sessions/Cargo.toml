[package]
name = "forge-sessions"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true
description = "What a view needs and nothing about how it renders."
publish = false

[lints]
workspace = true

[dependencies]
forge-primitives = { workspace = true }
forge-workspace = { workspace = true }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }

[dev-dependencies]
# Self-dep, so this crate's own `#[cfg(test)]` fixtures resolve
# `Workspace::new_for_test` and `install_db_for_test`.
forge-workspace = { path = "../forge-workspace", features = ["testing", "test-helpers"] }
tempfile = { workspace = true }
uuid = { workspace = true }
