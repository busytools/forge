[package]
name = "forge-gateway"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true
description = "The account pool: one backend per forge.toml provider token (credential resolution, probe HTTP + mapping, billing shape), plus account selection, health, probe scheduling and backoff."
publish = false

[lints]
workspace = true

[features]
# Test fixture helpers on the account state map, reached by
# forge-workspace's `testing` feature. Production builds never see it.
testing = []

[dependencies]
forge-primitives = { workspace = true }
async-trait = { workspace = true }
httpdate = { workspace = true }
reqwest = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
